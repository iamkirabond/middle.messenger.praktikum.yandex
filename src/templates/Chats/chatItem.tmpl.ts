export const ChatsItemTmpl = `<div class="chats-item clickable-chat" data-id={{{id}}}>
        <div class="image-placeholder clickable-chat">
            <img alt="" src={{{avatar}}}>
        </div>
        <div class="chat-info clickable-chat">
            <span class="chat-info-name">{{{title}}}</span>
        </div>
    </div>`;

/*
<button class="delete-chat-btn" id="deleteChat">x</button>

<p class="chat-info-last-msg">Изображение</p>

    <div class="chat-notifications">
        <span class="chat-last-date"></span>
        <span class="chat-new-msg">{{{unread_count}}}</span>
    </div>

*/
